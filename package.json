{
  "name": "gold",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production nodemon index",
    "start:dev": "cross-env NODE_ENV=development nodemon index",
    "test": "cross-env NODE_ENV=test jest --coverage --runInBand --verbose",
    "db:create:prod": "cross-env NODE_ENV=production sequelize db:create",
    "db:migrate:prod": "cross-env NODE_ENV=production sequelize db:migrate",
    "db:seed:prod": "cross-env NODE_ENV=production sequelize db:seed:all",
    "db:setup:prod": "npm run db:create:prod && npm run db:migrate:prod && npm run db:seed:prod",
    "db:create:dev": "cross-env NODE_ENV=development sequelize db:create",
    "db:migrate:dev": "cross-env NODE_ENV=development sequelize db:migrate",
    "db:seed:dev": "cross-env NODE_ENV=development sequelize db:seed:all",
    "db:setup:dev": "npm run db:create:dev && npm run db:migrate:dev && npm run db:seed:dev",
    "db:create:test": "cross-env NODE_ENV=test sequelize db:create",
    "db:migrate:test": "cross-env NODE_ENV=test sequelize db:migrate",
    "db:seed:test": "cross-env NODE_ENV=test sequelize db:seed:all",
    "db:setup:test": "npm run db:create:test && npm run db:migrate:test && npm run db:seed:test",
    "go:docker:terminal": "docker exec -t -i challengeplatinum-bej7-challengeplatinum bash"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "bcryptjs": "^2.4.3",
    "cloudinary": "^1.35.0",
    "cookie-parser": "^1.4.6",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jest": "^29.5.0",
    "joi": "^17.7.0",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "multer-storage-cloudinary": "^4.0.0",
    "passport-local": "^1.0.0",
    "pg": "^8.8.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.28.0",
    "sib-api-v3-sdk": "^8.5.0",
    "socket.io": "^4.6.1",
    "supertest": "^6.3.3",
    "swagger-ui-express": "^4.6.2",
    "uuidv4": "^6.2.13",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "sequelize-cli": "^6.6.0"
  }
}
